function mostrar()
{

}

/*            Intento fallido
	//a)
	var nota;
	var sumaNotas=0;
	var promedioNotas;
	//b)
	var notaMasBaja=0;
	var primeraVez=true;
	var genero;

	//c)
	var cantVaronesAprobados;

	//CONTADOR DE INGRESOS
	var cantidadIngresos=1;
	if(nota>=10 && nota <=0){

	while(cantidadIngresos<=5){

		//PIDO NOTA:
		nota=prompt("Ingrese una nota: ");

		//VALIDO NOTA:
		if(isNaN(nota)){
			nota=prompt("Error. Ingrese una nota: ");
		}

		nota=parseInt(nota);

		//DEFINO NOTA MAS BAJA:
		if(primeraVez){
			notaMasBaja=nota;
			primeraVez=false;
		}
		else{
			
			if(nota<notaMasBaja){
				notaMasBaja=nota;
			}

		}

		//PIDO GENERO:
		do{
			genero=prompt("Ingrese un genero, 'f' para femenino y 'm' para masculino: ");

		}while(genero!="f" && genero!="m");

		//PARA VERIFICAR SEXO Y NOTA MAS BAJA
		if(genero=="f"){
			generoNotaMasBaja="femenino";
		}

	}

	//PROMEDIO DE NOTAS:
	promedio=sumaNotas/cantidadIngresos
*/